include: package:flutter_lints/flutter.yaml
linter:
  rules:
    avoid_print: false
    # Onboarding uses async Supabase; patterns are valid with mounted checks.
    use_build_context_synchronously: false
